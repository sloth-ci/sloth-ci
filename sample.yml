name: test
listen_to: h/test

provider: dummy
provider_data:
    message: test

extensions:
    logs:
        log_dir: logs
        level: DEBUG

actions:
    - powershell Write-Host {message}
    - powershell Write-Host {foo}
    
params:
    foo: bar